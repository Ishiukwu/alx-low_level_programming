#include <stdio.h>

/**
 * main - prints out single digit num in base 10, starting from 0
 * Return: 0(Success)
 */
int main(void)
{
	int a;

	for (a = 0; a < 10; a++)
		printf("%i", a);
	printf("\n");
	return (0);
}

